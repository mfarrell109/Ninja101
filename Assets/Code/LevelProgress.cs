using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class LevelProgress : MonoBehaviour {
    /*
    public Rect scoreRect;
    //public static int score;        // The player's score.
    public Text text;               // Reference to the Text component.
    public GUIStyle guiStyle;
    //public PlayerMovement player;
    //public Texture2D progressBar;
    private AsyncOperation async;
    public float pc,hobar = 0;
   
    public Texture2D progressBar;


    void Update()
    {
        Debug.Log("loading progress: " + loadingScene.counter.ToString());

    }

    IEnumerator Start()
    {
        async = Application.LoadLevelAsync("Level3");
        yield return async;

    }
    


   void OnGUI()
    {
        
        guiStyle.fontSize = 10;
        GUI.Box(new Rect((Screen.width / 2) - 100, (Screen.height - 50), 100, 100), "Loading level percentage: " + loadingScene.counter.ToString(), guiStyle);

        if (async != null)
        {


            pc = async.progress * 100;
            hobar = pc * Screen.width / 100;

            GUI.DrawTexture(new Rect(0, Screen.height - 50, hobar, 50), progressBar, ScaleMode.StretchToFill);

        }
    }
    */




}
